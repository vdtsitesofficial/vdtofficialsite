{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "vdttest",
  "main": ".open-next/worker.js",
  "compatibility_date": "2025-01-01",
  "compatibility_flags": ["nodejs_compat"],
  "assets": {
    "directory": ".open-next/assets",
    "binding": "ASSETS"
  },
  // KV store for the /admin area: contact-form submissions, and later
  // editable blog posts + site copy.
  "kv_namespaces": [
    {
      "binding": "ADMIN_KV",
      "id": "c3690d7ca5fe41fdad5a6a7daec42775"
    }
  ],
  // One Worker, multiple custom domains. Cloudflare creates the DNS
  // automatically on deploy for each zone on this account. The site is
  // served identically on every hostname below. workers.dev stays live
  // as a backup.
  "workers_dev": true,
  "routes": [
    { "pattern": "vdtsites.ca", "custom_domain": true },
    { "pattern": "vdtsites.com", "custom_domain": true },
    { "pattern": "www.vdtsites.com", "custom_domain": true }
  ],
  // Cloudflare Workers Email Routing send_email binding. With no
  // destination_address set, the Worker may send to ANY address that is a
  // verified destination in Email Routing for this account (Email > Email
  // Routing > Destination Addresses). This powers both the contact-form
  // notification and the /admin sign-in PIN.
  //
  // Each admin email in ADMIN_EMAILS below MUST be verified as a
  // destination address, or its PIN email will not deliver.
  "send_email": [{ "name": "SEND_EMAIL" }],
  "vars": {
    // FROM address. Must live on a domain (or subdomain) that has Email
    // Routing enabled in this Cloudflare account. Since nothing else uses
    // vdtsites.ca mail, we point the FROM at the apex directly.
    "CONTACT_FROM_EMAIL": "VDT Test <noreply@vdtsites.ca>",
    // Displayed to the user as the contact destination; actual delivery
    // happens via the SEND_EMAIL binding above.
    "CONTACT_TO_EMAIL": "info@vdtsites.ca",
    // Allowlist of admin emails that may sign in to /admin. Comma-separated.
    // Replace with the real admin addresses (both must be verified Email
    // Routing destinations so their sign-in PIN can be delivered).
    "ADMIN_EMAILS": "admin@vdtsites.ca"
  }
}
