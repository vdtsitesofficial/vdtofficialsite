node_modules
.next
out
.open-next
.wrangler
tsconfig.tsbuildinfo
*.tsbuildinfo

# Local reference assets (mockup videos / screenshots dropped in the repo
# root for inspiration). Don't commit binaries to the project root.
/*.mp4
/*.mov
/*.webm
/videoframe_*.png

# Personal scratch dirs that don't belong in source control
/test/
/test
*.pdf
.env*.local
.DS_Store
*.log
.vercel
.deploy-secrets.tmp
*.deploy-secrets*

# dev/debug scratch (never ship)
/_render_*.js
/public/lab/_*
/public/lab/assets/*.old.png
/public/lab/assets/*.full.png

# responsive-audit sweep artifacts (screenshots + report JSON, regenerated per run)
responsive-audit-out/
